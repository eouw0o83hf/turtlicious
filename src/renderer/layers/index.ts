// ---------------------------------------------------------------------------
// Layer library — re-exports every available rendering-stack layer.
// Import individual layers from here to compose custom stacks.
// ---------------------------------------------------------------------------

export {
  DEFAULT_BRUSH_CONFIG,
  brushLayer,
  type BrushConfig,
  type BrushName,
  type SquareBrushOptions,
} from './brush';
